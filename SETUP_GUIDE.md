# 📘 راهنمای کامل نصب و راه‌اندازی

## گام 1: دریافت کلید API (اختیاری)

### روش A: استفاده از Claude AI (توصیه می‌شود)

1. به سایت https://console.anthropic.com بروید
2. ثبت‌نام کنید یا وارد شوید
3. به بخش "API Keys" بروید
4. یک کلید جدید بسازید
5. کلید را کپی کنید (شبیه: `sk-ant-api03-...`)

### روش B: استفاده از OpenAI

1. به سایت https://platform.openai.com بروید
2. ثبت‌نام کنید
3. به بخش "API Keys" بروید
4. یک کلید جدید بسازید
5. کلید را کپی کنید (شبیه: `sk-...`)

### روش C: بدون API (رایگان)

اگر کلید API ندارید، نگران نباشید! سیستم با پاسخ‌های پیش‌فرض کار می‌کند.

## گام 2: تنظیم کلید API

1. فایل `.env.example` را پیدا کنید
2. آن را کپی کرده و نامش را به `.env` تغییر دهید
3. فایل `.env` را با یک ادیتور متنی باز کنید
4. کلید API خود را جایگذاری کنید:

```bash
# اگر از Claude استفاده می‌کنید:
ANTHROPIC_API_KEY=sk-ant-api03-YOUR_KEY_HERE

# یا اگر از OpenAI استفاده می‌کنید:
OPENAI_API_KEY=sk-YOUR_KEY_HERE
```

5. فایل را ذخیره کنید

## گام 3: اجرا

### ویندوز
```
دابل کلیک روی run.bat
```

### Mac/Linux
```bash
chmod +x run.sh
./run.sh
```

وقتی برنامه اجرا شد، این پیام را خواهید دید:
```
🚀 Instagram DM Agent در حال اجرا...
📱 برای دسترسی به رابط کاربری به آدرس http://localhost:5000 بروید
 * Running on http://0.0.0.0:5000
```

## گام 4: باز کردن در مرورگر

1. مرورگر خود را باز کنید
2. به آدرس `http://localhost:5000` بروید
3. رابط کاربری ایجنت را خواهید دید

## گام 5: ورود به اینستاگرام

⚠️ **هشدار مهم:** استفاده از یوزرنیم/پسورد مستقیم ممکن است نقض قوانین Instagram باشد. این فقط برای تست و آموزش است.

1. روی دکمه "ورود به اینستاگرام" کلیک کنید
2. یوزرنیم اینستاگرام خود را وارد کنید
3. پسورد را وارد کنید
4. روی "ورود" کلیک کنید

**نکات مهم:**
- اگر Instagram از شما تایید هویت خواست، ابتدا از اپلیکیشن موبایل وارد شوید
- اگر خطای "Please Wait Few Minutes" دریافت کردید، 5-10 دقیقه صبر کنید
- برای امنیت بیشتر، از یک اکانت تست استفاده کنید

## گام 6: آموزش ایجنت

1. به صفحه "آموزش ایجنت" بروید
2. اطلاعات محصول خود را وارد کنید، مثلاً:

```
محصول: دوره آموزش برنامه‌نویسی Python
قیمت: 500,000 تومان
مدت: 3 ماه

ویژگی‌ها:
- 50 ساعت ویدیو آموزشی
- 20 پروژه عملی
- پشتیبانی تلگرامی
- گواهینامه معتبر

مخاطبان:
- مبتدی‌ها
- دانشجویان
- علاقه‌مندان به برنامه‌نویسی

مزایای خرید:
- پشتیبانی مادام‌العمر
- آپدیت‌های رایگان
- گروه تلگرامی اختصاصی
- 30 روز ضمانت بازگشت وجه
```

3. روی "ذخیره اطلاعات آموزشی" کلیک کنید

## گام 7: استفاده از ایجنت

### روش A: پاسخ دستی
1. به صفحه "دایرکت‌ها" بروید
2. روی یک مکالمه کلیک کنید
3. پیام خود را بنویسید و ارسال کنید

### روش B: پاسخ هوشمند AI
1. به صفحه "دایرکت‌ها" بروید
2. روی یک مکالمه کلیک کنید
3. روی دکمه "پاسخ هوشمند" کلیک کنید
4. ایجنت به صورت خودکار پاسخ می‌دهد

### روش C: پاسخگویی خودکار
1. به صفحه "تنظیمات" بروید
2. گزینه "پاسخگویی خودکار" را فعال کنید
3. از این به بعد، ایجنت خودکار به پیام‌ها پاسخ می‌دهد

## گام 8: مشاهده آمار

1. به صفحه "آمار" بروید
2. عملکرد ایجنت را مشاهده کنید:
   - تعداد مکالمات
   - تعداد پیام‌ها
   - مراحل فروش
   - نرخ تبدیل

## نمونه مکالمه فروش 5 مرحله‌ای

**مرحله 1 - آشنایی (پیام 1):**
```
سلام! 👋 خوشحالم که پیام دادی.
چطور می‌تونم کمکت کنم؟
```

**مرحله 2 - شناسایی نیاز (پیام 2):**
```
عالیه! 😊 دقیقاً به دنبال چی هستی؟
می‌خوای برنامه‌نویسی یاد بگیری؟
```

**مرحله 3 - ارائه راه‌حل (پیام 3):**
```
دوره Python ما دقیقاً برای تو مناسبه! 🎯
شامل 50 ساعت ویدیو + پروژه‌های عملی
می‌خوای بیشتر بدونی؟
```

**مرحله 4 - رفع اعتراضات (پیام 4):**
```
قیمتش 500 هزار تومان با پشتیبانی مادام‌العمر ✨
همین الان بیش از 500 نفر دارن یاد می‌گیرن!
سوال خاصی داری؟
```

**مرحله 5 - بستن فروش (پیام 5):**
```
پیشنهاد ویژه: الان ثبت‌نام کن، 20% تخفیف بگیر! 🎁
لینک پرداخت: example.com/buy
آماده‌ای شروع کنی؟
```

## عیب‌یابی مشکلات رایج

### خطا: "ModuleNotFoundError"
```bash
# مطمئن شوید محیط مجازی فعال است
source venv/bin/activate  # Mac/Linux
venv\Scripts\activate     # Windows

# سپس مجدد نصب کنید
pip install -r requirements.txt
```

### خطا: "Address already in use"
```bash
# پورت 5000 اشغال است، پورت را تغییر دهید:
# در فایل backend/app.py خط آخر را تغییر دهید:
app.run(debug=True, host='0.0.0.0', port=5001)
```

### خطا: "Login failed"
- مطمئن شوید یوزرنیم/پسورد صحیح است
- از اپلیکیشن موبایل Instagram وارد شوید
- 10 دقیقه صبر کنید و دوباره تلاش کنید
- VPN خود را خاموش کنید

### AI پاسخ نمی‌دهد
- بررسی کنید کلید API در `.env` درست است
- اعتبار API خود را چک کنید
- بدون API هم سیستم کار می‌کند (با پاسخ‌های پیش‌فرض)

### دیتابیس خطا می‌دهد
```bash
# حذف و ایجاد مجدد دیتابیس
rm database/instagram_agent.db
# برنامه را دوباره اجرا کنید
```

## سوالات متداول (FAQ)

**Q: آیا رایگان است؟**
A: بله! کد رایگان است. فقط اگر از AI استفاده کنید، باید API کلید بخرید.

**Q: آیا امن است؟**
A: برای تست بله، اما برای استفاده تجاری توصیه نمی‌شود.

**Q: چند اکانت می‌تونم مدیریت کنم؟**
A: فعلاً فقط یک اکانت. برای چند اکانت باید کد را تغییر دهید.

**Q: آیا پیام‌های من ذخیره می‌شود؟**
A: بله، در دیتابیس محلی شما (`database/instagram_agent.db`)

**Q: می‌تونم خودم کدش رو تغییر بدم؟**
A: بله! کد Open Source است. هر تغییری می‌خواهید بدهید.

**Q: Instagram اکانتم رو بن نمی‌کنه؟**
A: خطر وجود دارد. برای امنیت از یک اکانت تست استفاده کنید.

## نکات پیشرفته

### استفاده از چند زبان
فایل `backend/ai_service.py` را ویرایش کنید و زبان prompt را تغییر دهید.

### تغییر تعداد مراحل فروش
در صفحه "تنظیمات" می‌توانید از 3 تا 10 مرحله تنظیم کنید.

### اضافه کردن پیام‌های خودکار
فایل `backend/ai_service.py` را ویرایش کرده و در تابع `_generate_fallback_response` پیام‌های جدید اضافه کنید.

## پشتیبانی

اگر مشکلی دارید:
1. GitHub Issues: https://github.com/Juk3us/algorithm-research-project/issues
2. README.md را مطالعه کنید
3. کد را debug کنید

---

**موفق باشید! 🚀**
